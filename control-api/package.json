{
  "name": "reeds-ledger-control-api",
  "version": "1.0.0",
  "private": true,
  "description": "Secured owner API for the Reeds Ledger control app",
  "main": "server/index.js",
  "scripts": {
    "start": "node server/index.js",
    "check": "node --check server/index.js && node --check server/auth.js && node --check server/db.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.21.2",
    "pg": "^8.13.3"
  }
}
